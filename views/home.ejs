<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
</head>

<body>
    <h1>URL Shortner</h1>

    <% if (locals.shortId) { %>
        <p>URL Generated: http://localhost:8000/url/<%= locals.shortId %>
        </p>
        <% } %>

            <div>
                <form method="post" action="/url">
                    <label>Enter your URL: </label>
                    <input type="text" name="redirectUrl" placeholder="https://example.com" />
                    <button>Generate Short URL</button>
                </form>
            </div>

            <% if (locals.urls) { %>
                <table>
                    <thead>
                        <th>S. No</th>
                        <th>ShortId</th>
                        <th>Redirect URL</th>
                        <th>No of Clicks</th>
                    </thead>

                    <tbody>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td>
                                    <%= url.redirectUrl %>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <% } %>
</body>

</html>